<div class="container">
  <div class="row">
    <main class="col-md-9">
      {{#if year}}<h2 class="mt-5">{{#if month}}{{month}}/{{/if}}{{year}}</h2>{{/if}}
      {{#if posts.length}}
        {{#each posts}}
          {{> post this}}
        {{/each}}
        <p class="blog-pagination">
          Showing {{posts.length}} posts out of {{total}} total, page {{pageNum}}
        </p>
        <p class="blog-pagination">
          {{#if prevPage}}<a href="{{blogRoutePrefix}}{{pathPrefix}}/page/{{prevPage}}">Previous page</a>{{/if}}
          {{#if nextPage}}<a href="{{blogRoutePrefix}}{{pathPrefix}}/page/{{nextPage}}">Next page</a>{{/if}}
        </p>
      {{else}}
        {{> no-content}}
      {{/if}}
    </main>
    <aside class="col-md-3 sidebar">
      {{> sidebar}}
    </aside>
  </div>
</div>
